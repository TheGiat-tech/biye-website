# Site URL for sitemap and metadata
NEXT_PUBLIC_SITE_URL=https://biyecare.com

# Contact form email configuration
CONTACT_EMAIL=info@biyecare.com

# SMTP Configuration for sending emails
# For Gmail: smtp.gmail.com, 587, true (use App Password)
# For SendGrid: smtp.sendgrid.net, 587, true (use API key as password)
# For AWS SES: email-smtp.REGION.amazonaws.com, 587, true (use SMTP credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=true
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password-or-api-key

